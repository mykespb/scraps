scraps - Examples of web scraping
=================================

scrapy-test
---------------------------------

Using scrapy.

selenium-test
---------------------------------

Using selenium.
We assume that drivers for Chrome and/or Firefox are already installed in the system.

